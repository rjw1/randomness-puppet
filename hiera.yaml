version: 5
defaults:
  datadir: hieradata
  data_hash: yaml_data
hierarchy:
  - name: "yaml data"
    paths:
      - "node.%{::clientcert}.yaml"
      - "env.%{::environment}.yaml"
      - "common.yaml"
